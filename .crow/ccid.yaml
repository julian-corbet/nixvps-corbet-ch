# Replace b4224488319cc65987a023215251733b081c275d with one verified ccid commit before committing this adapter.
when:
  - event: manual
skip_clone: true
variables:
  SOURCE_ARCHIVE: {default: ""}
  SOURCE_SHA256: {default: ""}
  CI_TOOL_ARCHIVE: {default: ""}
  CI_TOOL_SHA256: {default: ""}
  CI_TOOL_BINARY: {default: ""}
  CI_TOOL_BINARY_SHA256: {default: ""}
  CHECKS: {default: "native"}
  CI_JOBS: {default: ""}
  CI_TEST_THREADS: {default: ""}
  CI_MEMORY_MB: {default: ""}
  CI_MEMORY_PER_JOB_MB: {default: ""}
  CI_TIMEOUT: {default: ""}
  CI_CACHE_ROOT: {default: ""}
  CI_NIX_JOBS: {default: ""}
  CI_LINKER: {default: system}
  TMPDIR: {default: ""}
steps:
  - name: selected-checks
    image: bash
    environment:
      CCID_REVISION: 'b4224488319cc65987a023215251733b081c275d'
    commands:
      - |
        set -eu
        printf '%s  %s\n' "$CI_TOOL_SHA256" "$CI_TOOL_ARCHIVE" | sha256sum --check --strict
        test "$(git get-tar-commit-id < "$CI_TOOL_ARCHIVE")" = "$CCID_REVISION"
        test -n "$CI_TOOL_BINARY"
        test -n "$CI_TOOL_BINARY_SHA256"
        printf '%s  %s\n' "$CI_TOOL_BINARY_SHA256" "$CI_TOOL_BINARY" | sha256sum --check --strict
        test "$("$CI_TOOL_BINARY" source-revision)" = "$CCID_REVISION"
        TMPDIR="${TMPDIR:-${CI_CACHE_ROOT:-${CARGO_HOME:-$HOME/.cargo}/ccid}/tmp}"
        export TMPDIR
        mkdir -p "$TMPDIR"
        ci_source_dir=""
        trap 'test -z "$ci_source_dir" || rm -rf -- "$ci_source_dir"' EXIT
        ci_source_dir="$(mktemp -d)"
        "$CI_TOOL_BINARY" verify-source --archive "$SOURCE_ARCHIVE" --sha256 "$SOURCE_SHA256" --commit "$CI_COMMIT_SHA" --destination "$ci_source_dir"
        "$CI_TOOL_BINARY" check --repo "$ci_source_dir" --check "$CHECKS"
